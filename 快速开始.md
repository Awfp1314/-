# 🚀 快速开始指南

## 第一步：安装依赖

打开命令提示符（CMD）或 PowerShell，进入项目目录：

```bash
cd c:\Users\wang\Desktop\sort
npm install
```

等待安装完成（大约1-3分钟）。

---

## 第二步：解析题库

运行以下命令，自动解析 `题库ProMax.md`：

```bash
npm run parse
```

成功后会显示：
```
✅ 解析完成！共 XXX 道题目
📊 题库统计:
   CC2530/ZigBee: XX 题
   CAN总线: XX 题
   ...
```

---

## 第三步：启动开发服务器

```bash
npm run dev
```

看到以下提示即成功：
```
  ➜  Local:   http://localhost:3000/
  ➜  Network: http://192.168.x.x:3000/
```

---

## 第四步：访问系统

打开浏览器，访问：**http://localhost:3000**

🎉 开始刷题吧！

---

## 🌐 部署到Ubuntu服务器

### 第一步：构建项目

```bash
npm run build
```

构建产物在 `dist/` 目录。

### 第二步：上传到服务器

**方法1：使用SCP**
```bash
scp -r dist/* root@your-server-ip:/var/www/iot-quiz/
```

**方法2：使用自动部署脚本**
1. 编辑 `deploy.sh`，修改服务器IP和用户名
2. 运行：`bash deploy.sh`

### 第三步：配置Nginx

登录服务器，创建配置：
```bash
sudo nano /etc/nginx/sites-available/iot-quiz
```

粘贴以下内容：
```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    root /var/www/iot-quiz;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

启用配置：
```bash
sudo ln -s /etc/nginx/sites-available/iot-quiz /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

### 第四步：访问网站

浏览器打开：**http://your-server-ip**

---

## ❓ 遇到问题？

### 安装依赖慢？

使用淘宝镜像：
```bash
npm install --registry=https://registry.npmmirror.com
```

### 题库没有显示？

1. 确保 `题库ProMax.md` 在项目根目录
2. 重新运行：`npm run parse`
3. 检查 `src/questionBank.js` 是否生成

### 服务器无法访问？

1. 检查防火墙是否开放80端口：
   ```bash
   sudo ufw allow 80
   ```

2. 检查Nginx状态：
   ```bash
   sudo systemctl status nginx
   ```

---

## 📞 需要帮助？

- 查看详细文档：`README.md`
- 提Issue：https://github.com/Awfp1314/iot-quiz-system
- 邮箱：feedback@iotmaster.com
